name: Deploy to EC2

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
    - name: Run deployment script
      run: |
        chmod +x deploy.sh
        ./deploy.sh
